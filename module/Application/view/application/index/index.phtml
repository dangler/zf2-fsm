<?php

$title = 'Login';
$this->headTitle($title);
?>

<?php
$form->setAttribute('action', $this->url('application', array('action' => 'index')));
$form->prepare();

?>
<?= $this->form($form, 'horizontal');